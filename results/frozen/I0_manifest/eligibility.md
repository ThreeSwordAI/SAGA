# I0 cohort — eligibility

Generated by `analysis/build_I0_manifest.py` from the committed run metadata; every number below is counted from `manifest.csv`, none is typed by hand. Re-render and diff to check it has not drifted (`tests/test_I0_manifest.py`).

## The historical feature stage

Every historical patch diagnostic in this project was computed at **`s12_pre_norm`** — the output of the LAST transformer block, **before** the final LayerNorm. Identified by reading the code, not assumed:

> saga/metrics.py:287-291 (forward hooks on model.blocks[i], storing the block's own output), saga/metrics.py:324-325 (feats[L-1][:, P:, :]), saga/vit.py:238-241 (SAGAViT.forward applies self.norm AFTER the block loop), tools/diagnose.py:89-95 (the historical driver)

`saga/frozen/stages.py` exposes that stage as the alias `hist`, and `manifest.csv`'s `hist_stage` column carries it for every row whose run has a `diag/` directory. Rows with no historical diagnostics (fine-tuning, dense heads) carry `MISSING`.

## Rows per family and status

| family | status | rows |
|---|---|---|
| e2r_300ep | eligible | 10 |
| e2r_300ep | invalid | 4 |
| e2r_300ep | superseded | 10 |
| legacy_300ep | eligible_legacy | 9 |
| legacy_300ep | invalid | 6 |
| legacy_300ep | superseded | 9 |
| ablation_100ep | eligible | 6 |
| ablation_100ep | superseded | 6 |
| finetune | eligible | 24 |
| finetune | superseded | 24 |
| dense_det | eligible | 3 |
| dense_det | superseded | 3 |
| dense_seg | eligible | 3 |
| dense_seg | superseded | 3 |
| ttr_edit | derived | 4 |
| **total** | | **124** |

## The 300-epoch classification cohort

**19 eligible conditions** (`family in {e2r_300ep, legacy_300ep}`, `ckpt_kind=last`, `status in {eligible, eligible_legacy}`), by cell:

| arch | recipe_actual | variant | conditions |
|---|---|---|---|
| vit_base | mixup | baseline | 2 |
| vit_base | mixup | registers | 1 |
| vit_base | mixup | saga | 2 |
| vit_small | mixup | baseline | 4 |
| vit_small | mixup | registers | 2 |
| vit_small | mixup | saga | 4 |
| vit_small | nomix | baseline | 2 |
| vit_small | nomix | saga | 2 |

By variant: **baseline** 8, **registers** 3, **saga** 8.

Cell membership is IMPORTED from `analysis/build_pooled_tables.py` (`CELLS`, `LEGACY_MEMBERS`, `E2R_MEMBERS`), so the recipe erratum remap and the exclusion of the VOID legacy ViT-B mixup-dir trio cannot drift between this manifest and `results/tables/e2_pooled.csv`. `recipe_actual` is never read from a directory name.

### Seed control

10 of the 19 eligible conditions were trained under the seeded e2r trainer (`seed_controlled=1`); the remaining 9 are legacy repeats whose trainer recorded no seed (`seed_controlled=0`, `seed=MISSING`). Seeds come from training metadata only: a diagnostics JSON's `seed` field is `tools/diagnose.py`'s evaluation default of 0 and is never read here.

## Excluded rows, with reasons

| run_id | ckpt_kind | status | reason |
|---|---|---|---|
| e2r_vitb_mixup_baseline_s2 | best | invalid | training incomplete: 1 of 300 epochs, end_time=None |
| e2r_vitb_mixup_baseline_s2 | last | invalid | training incomplete: 1 of 300 epochs, end_time=None |
| e2r_vitb_mixup_saga_s2 | best | invalid | training incomplete: 1 of 300 epochs, end_time=None |
| e2r_vitb_mixup_saga_s2 | last | invalid | training incomplete: 1 of 300 epochs, end_time=None |
| legacy_e2_vit_base_mixupdir_baseline | best | invalid | VOID: the legacy vit_base mixup-dir trio never finished training (last.pth at epoch 181 of 300); it is a member of no cell in analysis/build_pooled_tables.py and appears in no eligible cohort (results/notes/bmixup_forensics.md) |
| legacy_e2_vit_base_mixupdir_baseline | last | invalid | VOID: the legacy vit_base mixup-dir trio never finished training (last.pth at epoch 199 of 300); it is a member of no cell in analysis/build_pooled_tables.py and appears in no eligible cohort (results/notes/bmixup_forensics.md) |
| legacy_e2_vit_base_mixupdir_registers | best | invalid | VOID: the legacy vit_base mixup-dir trio never finished training (last.pth at epoch 246 of 300); it is a member of no cell in analysis/build_pooled_tables.py and appears in no eligible cohort (results/notes/bmixup_forensics.md) |
| legacy_e2_vit_base_mixupdir_registers | last | invalid | VOID: the legacy vit_base mixup-dir trio never finished training (last.pth at epoch 249 of 300); it is a member of no cell in analysis/build_pooled_tables.py and appears in no eligible cohort (results/notes/bmixup_forensics.md) |
| legacy_e2_vit_base_mixupdir_saga | best | invalid | VOID: the legacy vit_base mixup-dir trio never finished training (last.pth at epoch 89 of 300); it is a member of no cell in analysis/build_pooled_tables.py and appears in no eligible cohort (results/notes/bmixup_forensics.md) |
| legacy_e2_vit_base_mixupdir_saga | last | invalid | VOID: the legacy vit_base mixup-dir trio never finished training (last.pth at epoch 74 of 300); it is a member of no cell in analysis/build_pooled_tables.py and appears in no eligible cohort (results/notes/bmixup_forensics.md) |

`superseded` rows (every `best.pth` outside the fine-tuning family) are omitted from the table above for length; they are in `manifest.csv` with their own one-line reason. The fine-tuning family is the one exception: it selects on a frozen val split, so its `best.pth` is canonical and its `last.pth` is the superseded row.

## Prefix tokens

- `n_prefix = 1`: baseline, saga
- `n_prefix = 2`: baseline
- `n_prefix = 5`: registers

Counted from a model built through `tools/model_factory.py` at each run's own recorded `gate_mode`, then `saga.metrics.infer_num_prefix_tokens` — never a hard-coded 1 (audit bug B2). Register models are the timm model itself: `SAGAViT` refuses `num_prefix_tokens != 1` by design (`saga/vit.py:125-136`), and the frozen framework therefore never wraps them.

## Checkpoint hashes

120 of 124 rows carry a `ckpt_sha256`; **4** are `MISSING` and are filled by `tools/frozen_manifest_hashes.py` on the HPC, where the checkpoints live. `saga/frozen/runner.py` REFUSES to evaluate a row whose `ckpt_sha256` is still MISSING.

