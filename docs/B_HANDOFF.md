# TASK B handoff — does the learned gate arrangement matter, and do high-prevalence coordinates respond differently?

**GENERATED by `analysis/build_B_handoff.py` at git `a702663`.** Every number below is read from `results/frozen/I3_gate_edits/` and `results/frozen/I4_perturbation/`; none is typed by hand. The interpretation branch each contrast lands in is COPIED from `analysis/i34_contrasts.py` output — this generator chooses no sentence. Re-run it after new results land.

Analysis parameters frozen **2026-09-17** at git `5c1b737`; D5 masks sha256 `72612357be7dde3925b3a312b20964c826234396c165580d55f21e10e6bfe96e`.

## 1. The three primary contrasts

D7 named them before any evaluation number existed, and `docs/LOCKED_ANALYSIS.md` §9 fixes which two of I3's are primary. The decision rule (§10) is: direction consistent across the fresh ViT-S/mixup pair, image-level bootstrap CI (10,000 resamples, seed 0) excluding zero in each, effect size in nats and top-1 points.

- **C1** — verdict null
    - **layer 7** (2 deciding):
        - `e2r_vits_mixup_saga_s1` 0.00002 nats [-0.00034, 0.00039], Δtop-1 -0.036 pts
        - `e2r_vits_mixup_saga_s2` -0.00014 nats [-0.00042, 0.00014], Δtop-1 -0.019 pts
        - sign mixed; 0 of 2 intervals exclude zero
    - **layer 8** (2 deciding):
        - `e2r_vits_mixup_saga_s1` -0.00005 nats [-0.00023, 0.00013], Δtop-1 0.019 pts
        - `e2r_vits_mixup_saga_s2` -0.00035 nats [-0.00055, -0.00015], Δtop-1 0.012 pts
        - sign consistent (negative); 1 of 2 intervals exclude zero
    - branch: **no_arrangement**
- **C2** — verdict null
    - **layer 7** (2 deciding):
        - `e2r_vits_mixup_saga_s1` -0.00027 nats [-0.00042, -0.00013], Δtop-1 -0.019 pts
        - `e2r_vits_mixup_saga_s2` -0.00008 nats [-0.00019, 0.00004], Δtop-1 -0.013 pts
        - sign consistent (negative); 1 of 2 intervals exclude zero
    - **layer 8** (2 deciding):
        - `e2r_vits_mixup_saga_s1` 0.00003 nats [-0.00005, 0.00010], Δtop-1 0.006 pts
        - `e2r_vits_mixup_saga_s2` -0.00003 nats [-0.00011, 0.00005], Δtop-1 -0.008 pts
        - sign mixed; 0 of 2 intervals exclude zero
    - branch: **no_arrangement**
- **C3** — verdict null
    - **layer 7** (4 deciding):
        - `e2r_vits_mixup_baseline_s1` -0.00010 nats [-0.00022, 0.00001], Δtop-1 0.004 pts
        - `e2r_vits_mixup_baseline_s2` 0.00002 nats [-0.00009, 0.00013], Δtop-1 0.010 pts
        - `e2r_vits_mixup_saga_s1` -0.00010 nats [-0.00023, 0.00002], Δtop-1 -0.035 pts
        - `e2r_vits_mixup_saga_s2` 0.00000 nats [-0.00013, 0.00014], Δtop-1 -0.003 pts
        - sign mixed; 0 of 4 intervals exclude zero; θ(primary) inside the control band on 3 of 4
    - **layer 8** (4 deciding):
        - `e2r_vits_mixup_baseline_s1` -0.00008 nats [-0.00016, 0.00000], Δtop-1 0.008 pts
        - `e2r_vits_mixup_baseline_s2` -0.00012 nats [-0.00020, -0.00003], Δtop-1 0.013 pts
        - `e2r_vits_mixup_saga_s1` -0.00011 nats [-0.00019, -0.00003], Δtop-1 0.010 pts
        - `e2r_vits_mixup_saga_s2` -0.00005 nats [-0.00013, 0.00002], Δtop-1 -0.005 pts
        - sign consistent (negative); 2 of 4 intervals exclude zero; θ(primary) inside the control band on 0 of 4
    - branch: **unanticipated**

### Pre-declared secondary contrasts

`mean` − `original` (S1) and `permute` − `dihedral` (S2) are reported under the same rule with no branch of their own (LOCKED §9, TASK B §4).

| contrast | layer | run_id | nats | CI | verdict |
|---|---|---|---|---|---|
| S1 | 7 | `e2r_vits_mixup_saga_s1` | 0.00008 | [-0.00030, 0.00046] | null |
| S1 | 7 | `e2r_vits_mixup_saga_s2` | -0.00014 | [-0.00043, 0.00014] | null |
| S1 | 8 | `e2r_vits_mixup_saga_s1` | -0.00008 | [-0.00026, 0.00010] | null |
| S1 | 8 | `e2r_vits_mixup_saga_s2` | -0.00033 | [-0.00052, -0.00013] | null |
| S2 | 7 | `e2r_vits_mixup_saga_s1` | -0.00037 | [-0.00052, -0.00021] | negative |
| S2 | 7 | `e2r_vits_mixup_saga_s2` | -0.00018 | [-0.00029, -0.00006] | negative |
| S2 | 8 | `e2r_vits_mixup_saga_s1` | -0.00001 | [-0.00008, 0.00007] | null |
| S2 | 8 | `e2r_vits_mixup_saga_s2` | -0.00008 | [-0.00017, -0.00000] | null |

## 2. Energy, and why the comparison is fair

**I3 matches by STRATIFICATION** (LOCKED §3 item 7, amended): ΔNLL is reported within deciles of `delta_update_norm`, pooled over the edited conditions per checkpoint, so the families are compared at equal injected energy. **I4 matches PER IMAGE**: a control's `energy_target` is the primary's measured injected norm on the same image, and the achieved norm is recorded for every condition.

- I3 strata rows: 558
- I4 largest `energy_rel_error` observed: **2.489e-07** (tolerance 0.01)
- Unmatched controls (`*_e10f_*`) are reported beside the matched ones; the gap between them is the size of the energy confound, reported rather than removed.

## 3. The cross-method row

| layer | tag | baseline − SAGA (nats) | paired with |
|---|---|---|---|
| 7 | legacy-mixupdir | -0.00030 | `legacy_e2_vit_small_mixupdir_saga` |
| 7 | legacy-nomixdir | -0.00009 | `legacy_e2_vit_small_nomixdir_saga` |
| 7 | s1 | -0.00009 | `e2r_vits_mixup_saga_s1` |
| 7 | s2 | -0.00002 | `e2r_vits_mixup_saga_s2` |
| 8 | legacy-mixupdir | -0.00013 | `legacy_e2_vit_small_mixupdir_saga` |
| 8 | legacy-nomixdir | -0.00003 | `legacy_e2_vit_small_nomixdir_saga` |
| 8 | s1 | 0.00008 | `e2r_vits_mixup_saga_s1` |
| 8 | s2 | -0.00015 | `e2r_vits_mixup_saga_s2` |

Registers are n = 2 and are labelled on every row; nothing pools them with anything.

## 4. Recorded deviation

*Copied verbatim from `docs/TASK_B_I3_I4.md` §4, as §11 requires. This generator refuses to render without it.*

The §4 inversion was identified, and its corrected reading committed, at **`3a030f6` (11:03:27)**, before any contrast had been computed on real records. Between that commit and **`3b0ca2f` (11:14:13)**, C1 and C2 MEANS were computed **at layer 7 only**, on three checkpoints, as the `figures_data/frozen/I3_primary.npz` reproduction check, and appear in the session record:

| checkpoint | C1 | C2 |
|---|---|---|
| `e2r_vits_mixup_saga_s1` | +0.00002289 | −0.00027491 |
| `e2r_vits_mixup_saga_s2` | −0.00013621 | −0.00007828 |
| `legacy_e2_vit_small_mixupdir_saga` | −0.00034356 | −0.00004071 |

Bootstrap CIs at 50 resamples were computed INTERNALLY by `_summary` and were never printed or inspected. No verdict was computed, no decision rule applied, no secondary contrast evaluated, and layer 8 was not touched.

The four‑pattern mapping above is a completion of a logical correction, not a response to those values — the corrected middle branch predates them. It is nevertheless disclosed as written with them known, because the alternative is an attestation that is not true. A reader can judge it.

**Standing rule, effective 2026‑09‑17:** a reproduction check compares **per‑condition means, never contrasts**, and only for conditions selected by a fixed rule stated in the log BEFORE the check runs. For I4: verify `I4_primary.npz` against the parquet on `native`, `prim_e10_L7` and `ctrl0_e10m_L7`, for the first three run_ids in manifest order, and nothing else, before `analysis/i34_contrasts.py` is invoked.

## 5. What Track B does NOT settle

- **Frozen-model edits show what the trained model USES, not what a differently trained model would achieve.** I3 edits a checkpoint at inference and never retrains; what a scalar gate trained from scratch would do is Track T and is not claimed here.
- **I4's masks come from one cell.** D5 is defined for `vit_small|mixup` only, so no other architecture or recipe is in I4.
- **Both I4 primary masks are the same 16 coordinates.** The two cell mean maps correlate at ρ = 0.9985, so comparing I4's two sites is a comparison of DEPTH with the address held fixed — not of two addresses.
- **Registers are n = 2.** They are reported and labelled and never decide.
- **A null is a result.** A contrast whose CI includes zero is reported as a null with its interval; nothing is re-framed.

Generated 2026-09-17T17:31:58+00:00 from 480 I3 condition rows and 36 I4 contrast rows. I3 decisions: {'7': {'C1': 'null', 'C2': 'null', 'branch': 'no_arrangement', 'branch_text': 'the frozen model does not use its gate arrangement beyond the per‑head mean'}, '8': {'C1': 'null', 'C2': 'null', 'branch': 'no_arrangement', 'branch_text': 'the frozen model does not use its gate arrangement beyond the per‑head mean'}}. I4 decisions: {'7': {'baseline': 'null', 'branch': 'unanticipated', 'branch_text': 'the theta/control-band pattern is not one of the three branches TASK B §6 names; it is reported with the band, the intervals and no interpretation supplied', 'saga': 'null'}, '8': {'baseline': 'null', 'branch': 'unanticipated', 'branch_text': 'the theta/control-band pattern is not one of the three branches TASK B §6 names; it is reported with the band, the intervals and no interpretation supplied', 'saga': 'null'}}.
