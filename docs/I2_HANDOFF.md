# TASK I2 handoff — the terminal patch gate, and the stage every new patch diagnostic is reported at

**Status: COMPLETE (Phases A / B1 / C1 / B2 / C2). Nothing pending from the HPC.**  
GENERATED by `analysis/build_I2_handoff.py`. Every number below is read from
`results/frozen/I2_terminal/*/tables/`, the two `thresholds_cal.json` and
`docs/LOCKED_ANALYSIS.md`; none is typed by hand, and the D1 sentence is COPIED
out of §1 of that document rather than restated. Re-run the script after new
results land and this document updates itself.

## 0. The decision, as signed

> D1 = `s11_out`: on the calibration split, in the `vit_small|mixup` cell (n = 4 pairs), the SAGA-vs-baseline gap at the historical stage survives a terminal-gate bypass at 0.68–0.91 of its native size with every sign preserved, so the untrained terminal constant is a contributor but not the main one; the cosine diagnostics, however, retain only 0.09–0.20 of that gap one block earlier. The final-block patch diagnostics are therefore a mixture of what training did and what a readout-invisible constant does, and they are reported with the `term_1.00` and `s11_out` controls beside them, never alone.
>
> Signed Mahfuzur Rahman Chowdhury, 2026-09-16, a120f4f. Decided by `analysis/i2_decision.decide()` on the calibration split; the rule was committed before any result existed.

**D1 = `s11_out`.** Every new patch diagnostic in I1, I3, I4 and I5 is reported at that
stage. The final-block quantities are reported beside it with the `term_1.00`
and `s11_out` controls, never alone.

## 1. What I2 measured

Under a CLS-only loss the last block's patch gate receives no gradient and sits
at σ(0) = 0.5 in every SAGA checkpoint. I2 asked how much of the SAGA-vs-baseline
gap on the patch diagnostics that untrained constant produces, by overriding it
with four declared constants and measuring both the classifier and the patch
rows at three stages.

| | calibration | evaluation |
|---|---|---|
| split sha | `6b707eb39f934274…` | `7fdf5f9f2ace98ef…` |
| images | 2000 | 10000 |
| runs | 19 | 19 |
| conditions | `native` on all 19; `term_0.25/0.50/0.75/1.00` on the 8 SAGA | same |
| stages | `s11_out`, `hist`, `s12_post_norm` | same |

## 2. The architectural half, on the evaluation split

8 SAGA checkpoints × 4 constants × 10000 images:

| | measured |
|---|---|
| max abs logit difference vs native | **0** |
| min top-1 agreement with native | **1** |
| max mean abs ΔNLL | **0** |
| `native` ≡ `term_0.50` (φ_L = 0) | **8 of 8** checkpoints bit-identical |
| max abs Δ`cos_all` at `hist` under `term_1.00` | 0.1991 |

Proposition 2 holds **exactly, not approximately**: a CLS-only readout cannot
see the terminal patch gate, and φ_L = 0 is the weight-decay fixed point it was
predicted to be on every trained SAGA checkpoint in the cohort. The patch
diagnostics at the same stage move while the classifier does not — which is the
panel, and `figures_data/frozen/F5A_terminal.npz` carries it: the diagnostic
against the terminal constant, per pair, at both stages, with the zero line.

## 3. The decision rule, and what evaluation says about it

`analysis/i2_decision.decide()` was committed in Phase A, before any result existed. It votes on the `vit_small\|mixup` cell (4 pairs) at a conventional survival cutoff of 0.70.

**D1 was decided on CALIBRATION: branch 3, `s11_out`.** It was then run ONCE on the evaluation tables as a
consistency check and returned **branch 3, `s11_out`** — the same branch. That check is reported, not acted on: D1 is signed on calibration by design.

| diagnostic | survival cal | survival eval | survival_s11 cal | survival_s11 eval |
|---|---|---|---|---|
| `cos_all` | 0.680 **✗** | 0.680 | 0.088 | 0.088 |
| `cos_nosink_mad` | 0.694 **✗** | 0.694 | 0.195 | 0.193 |
| `eff_rank` | 0.877 | 0.877 | 0.734 | 0.731 |
| `count_fixed_cal` | 0.914 | 0.914 | 0.976 | 0.968 |
| `count_mad` | 0.909 | 0.941 | 0.978 | 0.969 |

The same 2 diagnostics fail the bypass test on both splits (`cos_all`, `cos_nosink_mad`), and every survival agrees to within 0.032.

The non-voting cells, on both splits:

| cell | n | branch cal | branch eval |
|---|---|---|---|
| `vit_base\|mixup` | 2 | 1 | 1 |
| `vit_small\|nomix` | 2 | 3 | 3 |

## 4. Thresholds

`tau_canon` is the historical per-cell value, calibrated at the last block on
the DISCOVERY split; it is defined at `hist` and nowhere else, so `count_fixed_canon`
is the literal `MISSING` at every other stage. `tau_cal` re-runs the SAME recipe
(`tools/compute_fixed_thr.per_image_mad_thresholds`) per stage on the split being
evaluated, from each cell's canon-designated baseline.

| cell | `tau_canon` | `tau_cal[hist]` cal | `tau_cal[hist]` eval | `tau_cal[s11_out]` eval | `tau_cal[s12_post_norm]` eval |
|---|---|---|---|---|---|
| `vit_base\|mixup` | 127.3125 | 127.81152 | 127.26842 | 96.32041 | 14.67191 |
| `vit_small\|mixup` | 20.8515625 | 20.90086 | 20.90705 | 18.69855 | 18.13143 |
| `vit_small\|nomix` | 22.859375 | 22.77754 | 22.82821 | 19.93828 | 20.78440 |

## 5. `s12_post_norm` is not a candidate reporting stage

53 of the 176 rows of `T_I2d_maps.csv` carry `MISSING` for every spatial
statistic, all of them at `s12_post_norm`, because the exceedance map there has ZERO
mass. After the final LayerNorm the patch norms are close to uniform, so nothing
exceeds either threshold and a map with no mass has no spatial arrangement to
correlate. For one baseline on this split:

| run | stage | `norm_p50` | `norm_max` | `count_mad` | `count_fixed_cal` |
|---|---|---|---|---|---|
| `e2r_vits_mixup_baseline_s1` | `hist` | 14.240 | 94.659 | 18.299 | 19.670 |
| `e2r_vits_mixup_baseline_s1` | `s11_out` | 12.399 | 92.766 | 17.655 | 20.369 |
| `e2r_vits_mixup_baseline_s1` | `s12_post_norm` | 15.836 | 16.965 | 0.005 | 0.000 |

That is a property of the stage, not a gap in the results.

## 6. Where everything lives

| what | path |
|---|---|
| conditions (the only place they are defined) | `configs/frozen/I2_terminal.yaml` |
| job file | `scripts/jobs/frozen_I2.sbatch` (the split is an ARGUMENT) |
| per-split sweep | `results/frozen/I2_terminal/<split_name>/<run_id>/` |
| per-stage thresholds | `results/frozen/I2_terminal/<split_name>/thresholds_cal.json` |
| tables | `results/frozen/I2_terminal/<split_name>/tables/T_I2a-e*.csv` |
| the decision, and the note behind it | `analysis/i2_decision.py`, `results/frozen/I2_terminal/D1_proposal.md` |
| figure data | `figures_data/frozen/F5A_terminal.npz` |
| per-image primary diagnostics (evaluation) | `figures_data/frozen/I2_evaluation_primary.npz` |
| tests | `tests/test_I2_terminal.py` |

The evaluation `records.parquet` / `diag.parquet` stay on the cluster (19 runs ×
10,000 images). Each run's committed `run_meta.json` records their sha256 and
byte size, and the primary pack above carries the per-image values the
bootstrap resamples — so every number in these tables regenerates from the
repository.

## 7. What I2 does NOT settle

- I2 is descriptive. It shows a patch statistic moving while the classifier does
  not; it establishes no mechanism, and it cannot on its own justify acceptance.
- The 0.70 survival cutoff is conventional. §3(a) of `D1_proposal.md` records how
  close the branch was and what cutoff would have changed it.
- `D5` is still open in `docs/LOCKED_ANALYSIS.md` and still blocks I4: the
  prevalence map at the INPUT to block 7/8 does not exist, and I1 owns it.
- `docs/LOCKED_ANALYSIS.md` remains a DRAFT. D1-D4 and D6-D8 are closed; freezing
  the document is a separate act.
