# TASK 08 — fine-grained transfer, clean protocol: handoff

GENERATED by `analysis/build_task08_handoff.py` at git `ac7ceae`. Every experimental number below is read from `results/tables/T3_finegrained.csv` and the per-run `results/runs/ft_*/eval/test_final.json`; none is typed by hand. The surrounding prose is fixed text.

## TL;DR

The legacy fine-grained result was an artefact of selecting on the test split. Under an honest protocol the effect is **dataset-dependent, not a uniform win**:

| cell | SAGA − baseline (test top-1) | n pairs | clears 2×SE? |
|---|---|---|---|
| CUB-200-2011 ViT-S | **-0.362** | 3 | yes |
| CUB-200-2011 ViT-B | **+1.329** | 1 | not computable (n=1) |
| FGVC-Aircraft ViT-S | **+0.960** | 3 | yes |
| FGVC-Aircraft ViT-B | **+1.800** | 1 | not computable (n=1) |

The two ViT-S cells reach 2×SE significance **in opposite directions**. No pooled cross-dataset claim is made anywhere in this task's artifacts, and none should be.

The legacy headline deltas (**+2.19 Aircraft, +1.29 CUB**) are **VOID**: they must never be cited, and must never be compared against the table above, because they measure a different and invalid quantity. See §4.

## 1. Status

| phase | what | state |
|---|---|---|
| A | protocol + trainer + splits tooling + matrix + launchers + tests | **done** (`1a11988`) |
| B | HPC: build splits, smoke, 16 fine-tune runs | **done** (`d43a182`, `2d2c525`, `fd98185`) |
| C | T3 table + generated note | **done** (`61e3699`) |

Nothing is pending from the HPC for TASK 08. Its commits, in order:

| commit | phase | subject |
|---|---|---|
| `1a11988` | A | [TASK-08] fine-grained clean protocol (phase A) — *phase A: protocol, trainer, matrix, launchers, tests* (on `main`: yes) |
| `d43a182` | B | [TASK-08] frozen fine-grained val splits (CUB + Aircraft, seed 0) — *frozen val splits, built + committed on the HPC* (on `main`: yes) |
| `2d2c525` | B | T7 and T8 erros — *human sweep: the 2 runs that survived attempt 1* (on `main`: yes) |
| `fd98185` | B | T8 and T7 — *human sweep: the remaining 14 runs* (on `main`: yes) |
| `dd73aea` | B | [TASK-08] log: Phase B attempt 1 — 2/16 completed, 14 lost to the a0801 TaskProlog fault, resubmitted as 4198114 — *incident log: the a0801 node fault* (on `main`: yes) |
| `61e3699` | C | [TASK-08] T3 fine-grained table + note (phase C) — *phase C: T3 table + finegrained note* (on `main`: yes) |

Two of those commits (`2d2c525` "T7 and T8 erros", `fd98185` "T8 and T7") are the human's own repo-wide sweeps from the HPC and carry TASK-07 material as well; the TASK-08 payload in them is the `results/runs/ft_*/` directories. TASK 08 predates the one-branch-per-task rule in CLAUDE.md, so all six commits are directly on `main` (see §13).

## 2. The question this task had to settle

Does SAGA transfer — does a SAGA-pretrained backbone fine-tune to better fine-grained accuracy than a matched baseline backbone — measured under a protocol that cannot flatter it?

The prior answer in this repo was **not measurable**, because the number it rested on had been selected on the test split.

## 3. What we did (the protocol)

- **Val split.** A frozen, committed, seeded stratified 10% carve of the OFFICIAL TRAIN split, stored in full (relative paths + labels) in `results/ftsplit/<dataset>_val_split.json`. Built once on the HPC, write-once thereafter.
- **Selection.** Validated EVERY epoch on that carve; `best.pth` selected on it.
- **Test.** The official test split is touched **exactly once per run**, after training, with the val-selected `best.pth` reloaded → `eval/test_final.json`. It is constructed in exactly one place in the code (`final_test_eval`), a second call is refused, and a completed run is skipped on resubmission.
- **Backbones.** The seeded e2r **mixup s1** checkpoints (`last.pth`), strict-loaded with zero missing/unexpected keys, sha256 asserted against `configs/ft_matrix.yaml` — which is itself test-pinned to the committed e2r eval JSONs.
- **Hyperparameters MIRROR the legacy e6 trainer** (AdamW backbone 1e-5 / head 1e-3, wd 0.05, cosine to 1e-7, 100 epochs, batch 64, label smoothing 0.1, grad clip 1.0, fp16 AMP, legacy transforms, 224px). An adversarial mirror review found **zero unintended mathematical deviations**: only the protocol changed.
- **Resolution.** 224 only; any other `img_size` is refused, because the backbones (and SAGA's φ = [H, 196] gate grid) were trained on that grid and the interpolation path is unverified for this protocol.
- **Hygiene.** Per-run `ft_seed` seeding (separate from the backbone seed), `run_registry` provenance, append-safe 4-field `log.csv`, atomic checkpoint writes, a validity-checked completion marker, and a `run.lock` with atomic stale takeover against double submission. **No resume machinery** — runs are 20–40 min, far below the wall, so an interrupted run restarts fresh.

## 4. What was wrong before (bug B7)

`evaluation/e6_finegrained/tools/train.py` (the legacy trainer, still in the tree, never run by this task):

1. used the **official TEST split as its validation set**;
2. selected `best.pth` on it;
3. evaluated it **every 5 epochs**, so the reported peak was a maximum taken over ~20 looks at the test set;
4. loaded backbones with `strict=False` without checking unexpected keys.

Its headline deltas **+2.19 Aircraft / +1.29 CUB are VOID**. They are quoted in this repo only to record that they are retired.

> **Trap for the unwary.** The new CUB ViT-B single-seed delta happens to land near the void legacy CUB number. That is a coincidence across a different backbone, protocol and split. It is **not** corroboration, and the generated note forbids the comparison.

## 5. How to read these numbers (read before quoting any)

1. **Pairing is by ft-seed.** Baseline and SAGA at the same `ft_seed` share the frozen val split and the data order, so the paired delta is the meaningful comparison. Unpaired means are also given; an unpaired Welch test is reported as a robustness line only.
2. **`significant_2xSE` means |mean Δ| > 2×SE over n paired seeds.** With n = 3 that is a direction indicator, not an effect-size estimate. Both ViT-S verdicts clear the threshold, one of them thinly; the note prints each verdict's margin so "YES" cannot be read as "large". Welch does **not** reach significance in either ViT-S cell — expected, since it discards the pairing.
3. **`MISSING` is literal and never averaged.** n=1 cells have no std, no SE and no significance verdict; a run whose backbone sha does not match the matrix, or that is a smoke artefact, yields MISSING rather than an unverified number.
4. **Diagnostics come from the val-selected `best.pth`**, not the final epoch. Which matters: see §8.

## 6. Results — T3

Repeats are listed individually before any aggregate. Every value is the official test split, evaluated once, with the val-selected checkpoint.

### CUB-200-2011 — ViT-S (test n = 5794)

| variant | ft-seed | test top-1 | test top-5 | val top-1 @ best | best epoch |
|---|---|---|---|---|---|
| baseline | f0 | 81.153 | 95.461 | 82.833 | 23 |
| baseline | f1 | 80.739 | 94.684 | 83.167 | 58 |
| baseline | f2 | 80.739 | 96.117 | 82.667 | 15 |
| saga | f0 | 80.860 | 94.667 | 83.167 | 42 |
| saga | f1 | 80.532 | 95.185 | 83.500 | 32 |
| saga | f2 | 80.152 | 94.149 | 82.833 | 68 |

- baseline mean = **80.877** (n = 3, std = 0.2390)
- saga mean = **80.515** (n = 3, std = 0.3543)
- **paired Δ = -0.362** over 3 pair(s) [f0 -0.293, f1 -0.207, f2 -0.587]; SE = 0.1150; clears 2×SE: **YES**
  - margin |Δ| − 2×SE = +0.1322 (threshold 0.2301)
  - unpaired Welch: t = -1.4684, p = 0.2255

### CUB-200-2011 — ViT-B (test n = 5794)

| variant | ft-seed | test top-1 | test top-5 | val top-1 @ best | best epoch |
|---|---|---|---|---|---|
| baseline | f0 | 80.549 | 95.685 | 84.333 | 14 |
| saga | f0 | 81.878 | 95.841 | 83.167 | 11 |

- baseline mean = **80.549** (n = 1, std = MISSING)
- saga mean = **81.878** (n = 1, std = MISSING)
- **paired Δ = +1.329** over 1 pair(s) [f0 +1.329]; SE = MISSING; clears 2×SE: MISSING
  - **single ft-seed: no std, no SE, no significance claim is possible for this cell** (addressed later — see §10)

### FGVC-Aircraft — ViT-S (test n = 3333)

| variant | ft-seed | test top-1 | test top-5 | val top-1 @ best | best epoch |
|---|---|---|---|---|---|
| baseline | f0 | 72.757 | 92.739 | 73.857 | 54 |
| baseline | f1 | 74.197 | 93.309 | 75.000 | 76 |
| baseline | f2 | 72.247 | 92.859 | 73.857 | 55 |
| saga | f0 | 73.687 | 93.129 | 76.000 | 59 |
| saga | f1 | 74.407 | 93.339 | 74.000 | 68 |
| saga | f2 | 73.987 | 92.859 | 74.714 | 86 |

- baseline mean = **73.067** (n = 3, std = 1.0113)
- saga mean = **74.027** (n = 3, std = 0.3617)
- **paired Δ = +0.960** over 3 pair(s) [f0 +0.930, f1 +0.210, f2 +1.740]; SE = 0.4419; clears 2×SE: **YES**
  - margin |Δ| − 2×SE = +0.0761 (threshold 0.8839)
  - unpaired Welch: t = 1.5482, p = 0.2367

### FGVC-Aircraft — ViT-B (test n = 3333)

| variant | ft-seed | test top-1 | test top-5 | val top-1 @ best | best epoch |
|---|---|---|---|---|---|
| baseline | f0 | 72.427 | 93.129 | 72.143 | 19 |
| saga | f0 | 74.227 | 92.769 | 74.143 | 63 |

- baseline mean = **72.427** (n = 1, std = MISSING)
- saga mean = **74.227** (n = 1, std = MISSING)
- **paired Δ = +1.800** over 1 pair(s) [f0 +1.800]; SE = MISSING; clears 2×SE: MISSING
  - **single ft-seed: no std, no SE, no significance claim is possible for this cell** (addressed later — see §10)

## 7. Verification that was run before any table was built

All 16 runs in the table were checked: `smoke` false everywhere, `n_images` correct per dataset, 100 epochs and 100 log rows each, `best_epoch` and `val_top1_at_best` matching each run's OWN `log.csv` peak exactly, the four backbone sha256s matching `configs/ft_matrix.yaml`, and ONE frozen split sha per dataset across all its runs.

Every T3 value was then **independently recomputed straight from the run JSONs through a separate code path** (repeats, means, sample stds, paired deltas, SEs, 2×SE verdicts): **0 discrepancies**.

Three note-generator defects were found and fixed before commit: binomial-SE lines duplicated per cell instead of per dataset; a "n = 1 pairs" plural; and a bare "significant: YES" that could be misread as favourable on the cell where SAGA is *worse* — verdicts now print direction and margin.

## 8. Sanity findings (these are results, not bookkeeping)

**Every run overfits, and val-selection caught it.** 16 of 16 runs peaked on val strictly before their final epoch, with best epochs spanning 11..86 of 100. The final-epoch weights were never the val-best weights. This is exactly what the legacy test-tuned peak concealed, and it also settles the 100-vs-30-epoch question raised in Phase A: 100 epochs is harmless because selection is honest, whereas truncating to 30 would have cut off the runs that peaked later.

**Val is optimistic relative to test, as it must be.** 6 of 16 runs exceed the 2-point val−test flag; 13 of 16 gaps are positive; range -0.407 .. +3.784, mean +1.553. The note records this against each val split's own computed binomial noise floor (CUB n=600 ⇒ ≈1.53 pts; Aircraft n=700 ⇒ ≈1.65 pts) and notes that a val-selected maximum over 100 epochs is upward-biased by construction. **Reported, not corrected for** — the test numbers are unaffected, since the test split is evaluated once and never selected on.

Largest gaps:

| run | val top-1 @ best | test top-1 | val − test |
|---|---|---|---|
| cub ViT-B baseline f0 | 84.333 | 80.549 | +3.784 |
| cub ViT-S saga f1 | 83.500 | 80.532 | +2.968 |
| cub ViT-S saga f2 | 82.833 | 80.152 | +2.681 |
| cub ViT-S baseline f1 | 83.167 | 80.739 | +2.428 |
| aircraft ViT-S saga f0 | 76.000 | 73.687 | +2.313 |
| cub ViT-S saga f0 | 83.167 | 80.860 | +2.307 |

## 9. What we did NOT do (limitations, by design or by scope)

| not done | why | consequence |
|---|---|---|
| **registers variant** never fine-tuned | the matrix is `{baseline, saga}` only, per the task | T3 says nothing about registers on fine-grained transfer |
| **one backbone seed** (`bs1` = e2r mixup s1) | task-specified | every delta is conditional on one pretraining seed; backbone-seed variance is unmeasured and is NOT in the reported SE |
| **nomix backbones** not transferred | out of scope | transfer is measured only for mixup-pretrained backbones |
| **ViT-B at n=1** | 4 runs, not 12, per the task matrix | the +1.33 / +1.80 deltas had no error bars (now addressed — §10) |
| **only two datasets** | CUB + Aircraft were the staged ones | no evidence on whether the dataset-dependence generalizes |
| **no mechanism test** | TASK 08 measured *whether*, not *why* | the opposite-direction ViT-S result is unexplained by this task |
| **no resume machinery** | runs are 20–40 min | an interrupted run restarts from scratch, by choice; documented in `meta.json` |
| **GPU kernels not forced deterministic** | matches the e2r trainer's policy | a rerun may differ in the last digits; the seed is recorded, determinism is pinned only on CPU |
| **legacy e6 trainer left in place** | provenance | `evaluation/e6_finegrained/tools/train.py` still exists and still has bug B7 — do not run it |
| **`best.pth` / `last.pth` not in git** | size | they live ONLY on the HPC; anything needing them (e.g. a later ablation) depends on them not having been pruned |

One deliberate protocol wrinkle, recorded rather than hidden: the 2-epoch **smoke run** also evaluates the official test split once. It is flagged `smoke: true`, written to the git-ignored `results/smoke/`, excluded from every table by an explicit check, and test-pinned so it can never enter a result. But for the CUB ViT-S SAGA f0 configuration the test set was technically touched twice across the two runs.

## 10. IMPORTANT — the table is complete for TASK 08, but the matrix has since grown

`configs/ft_matrix.yaml` now declares **24 runs**, and **24** have a `test_final.json` on disk. The committed `T3_finegrained.csv` quoted above covers **16** — TASK 08's own sixteen.

The 8 additional runs are TASK 13's ViT-B seed fill (matrix indices 16-23), which exist to give the ViT-B cells the error bars TASK 08 could not:

- `ft_aircraft_vitb_baseline_bs1_f1`
- `ft_aircraft_vitb_baseline_bs1_f2`
- `ft_aircraft_vitb_saga_bs1_f1`
- `ft_aircraft_vitb_saga_bs1_f2`
- `ft_cub_vitb_baseline_bs1_f1`
- `ft_cub_vitb_baseline_bs1_f2`
- `ft_cub_vitb_saga_bs1_f1`
- `ft_cub_vitb_saga_bs1_f2`

**Consequences, for whoever picks this up:**

1. **Re-running `analysis/build_ft_tables.py` right now will NOT reproduce the committed table** — it reads the matrix, so it will emit a larger table in which the ViT-B cells have n=3 and therefore carry std/SE/significance where the committed table says MISSING. That is an improvement, not a bug, but it means the table is no longer byte-reproducible from the current matrix.
2. **Integrating those runs is TASK 13's Phase C, not TASK 08's.** Until it runs, the ViT-B rows in §6 are the committed state and the eight extra runs' numbers live only in their own `test_final.json` files (§11).
3. The **ViT-S rows are unaffected** — every one of the 8 added runs is ViT-B, so only those cells move. (Verified by rebuilding to a scratch path and diffing: 0 ViT-S rows change, every ViT-B row does.)

A regression test in `tests/test_task08_ft.py` asserts the committed table matches its source JSONs and expects exactly 16 repeat rows; if a rebuilt 24-run table is committed, that expectation is what will (correctly) fail and needs updating with it.

Separately, TASK 13 has produced **24 ring-ablation JSONs** under `results/finegrained/ring_ablation/`, an eval-only masking test over these fine-tuned checkpoints that probes *why* the direction differs by dataset. Those are TASK 13's results, not TASK 08's, and are not summarized here.

## 11. Which files hold which results

### Results (read numbers from these)

| file | what is in it |
|---|---|
| `results/tables/T3_finegrained.csv` | **the T3 table**, long format: one row per (dataset, arch, variant, kind). `kind` ∈ {repeat, mean, std, paired_delta, paired_delta_mean, paired_delta_se, significant_2xSE, welch_t, welch_p}. Columns: test_top1, test_top5, val_top1_at_best + per-repeat context (best_epoch, epochs_trained, val_minus_test, n_test_images) |
| `results/notes/finegrained.md` | the generated results note: the void-legacy statement, the protocol, the per-cell tables, the sanity paragraph, and a per-run provenance table |
| `results/runs/ft_<ds>_<arch>_<variant>_bs1_f<seed>/eval/test_final.json` | **the authoritative per-run number.** top1, top5, n_images, best_epoch, val_top1_at_best, epochs_trained, backbone_run/ckpt/sha256, finetuned_sha256, val_split + sha256, ft_seed (= seed), git_sha. Also the run's completion marker |
| `.../log.csv` | per-epoch `epoch, lr, train_loss, val_top1` — the val curve behind every `best_epoch` |
| `.../meta.json` | run provenance: git sha + dirty flag, command, host, GPU, library versions, start/end time, ft_seed, backbone and split shas, split sizes, the explicit no-resume and determinism notes |
| `.../config.resolved.yaml` | the fully resolved run config |
| `results/ftsplit/{cub,aircraft}_val_split.json` | **the frozen val carve**: full `items_train` and `items_val` (relative path + label), seed, val_frac, counts, n_classes |

Run directories in this task, by cell:

| cell | runs |
|---|---|
| CUB-200-2011 ViT-S | `ft_cub_vits_baseline_bs1_f0`, `ft_cub_vits_baseline_bs1_f1`, `ft_cub_vits_baseline_bs1_f2`, `ft_cub_vits_saga_bs1_f0`, `ft_cub_vits_saga_bs1_f1`, `ft_cub_vits_saga_bs1_f2` |
| CUB-200-2011 ViT-B | `ft_cub_vitb_baseline_bs1_f0`, `ft_cub_vitb_saga_bs1_f0` |
| FGVC-Aircraft ViT-S | `ft_aircraft_vits_baseline_bs1_f0`, `ft_aircraft_vits_baseline_bs1_f1`, `ft_aircraft_vits_baseline_bs1_f2`, `ft_aircraft_vits_saga_bs1_f0`, `ft_aircraft_vits_saga_bs1_f1`, `ft_aircraft_vits_saga_bs1_f2` |
| FGVC-Aircraft ViT-B | `ft_aircraft_vitb_baseline_bs1_f0`, `ft_aircraft_vitb_saga_bs1_f0` |

### Code

| file | what |
|---|---|
| `evaluation/e6_finegrained/tools/train_ft.py` | the clean trainer: protocol, single-touch test eval, locking, marker validity, strict load, seeding |
| `evaluation/e6_finegrained/tools/build_ft_split.py` | builds a frozen val carve (write-once); runs on a login node, reads only tar metadata |
| `evaluation/e6_finegrained/data/ft_meta.py` | THE single parsing path for official splits + label conventions (CUB `cls−1`, Aircraft `variants.txt` order) and the legacy transforms — shared by builder and trainer so labels cannot drift |
| `configs/ft_matrix.yaml` | run matrix + mirrored hyperparameters + backbone shas. **Row order is load-bearing** (the array job maps index → run by position): append only |
| `analysis/build_ft_tables.py` | T3 builder |
| `analysis/build_finegrained_note.py` | note renderer |
| `analysis/build_task08_handoff.py` | this document |
| `scripts/gen_ft_jobs.py` | generates the launchers |
| `scripts/jobs/ft_finegrained_array.sbatch` | single-GPU array job, per-task `/scratch` staging |
| `scripts/jobs/ft_smoke.sbatch` | 2-epoch smoke → `results/smoke/` |
| `tests/test_task08_ft.py` | 25 tests (CPU, fake data): split determinism/stratification/disjointness, label conventions, strict-load raises, sha-mismatch refusal, non-224 refusal, matrix contract, end-to-end run, test-touched-once, torn-marker recovery, lock ownership, write-once builder, T3 statistics, committed-table-vs-JSONs |
| `evaluation/e6_finegrained/tools/train.py` | **the VOID legacy trainer.** Kept for provenance. Has bug B7. Do not run |

## 12. Regenerate (all local, no GPU, no HPC)

```bash
python analysis/build_ft_tables.py          # see the §10 warning
python analysis/build_finegrained_note.py
python analysis/build_task08_handoff.py
pytest -q
```

Rebuilding a val split is deliberately refused if the file exists — the carve is the protocol, and changing it would invalidate every run. Deleting one to force a rebuild means every downstream run must be redone.

## 13. Phase-B operational history (worth knowing before the next array job)

The first submission of the 16-run array (job `4196932`) came back with **2 COMPLETED and 14 FAILED at `00:00:00` elapsed, exit `1:0`**. The cause was not in this repo:

```
error: run_command: slurm task_prolog can not be executed (/etc/slurm/slurm.taskprolog) Permission denied
error: TaskProlog failed status=1
```

Node **a0801** had an unreadable SLURM task prolog, so every task placed there died before the job script ran a single line. The correlation was exact: `Node list` was a0801 for all 14 failures and only for those; the 2 survivors ran on a0804/a0805. Resubmitting as `sbatch --exclude=a0801 scripts/jobs/ft_finegrained_array.sbatch` (job `4198114`) completed all 14 in 19–40 min each.

Two things paid off and are worth reusing: the completion-marker requeue guard meant the resubmission **skipped the 2 finished runs automatically** rather than redoing or corrupting them; and because the 14 failures never reached `create_run`, the total absence of `meta.json` for them was itself the proof that they died before training, which is what located the fault. **Diagnostic rule: `00:00:00` elapsed with exit `1:0` means look at the node, not the code.**

## 14. Provenance warnings for whoever picks this up

- **TASK 08's commits are on `main`, not a task branch.** CLAUDE.md gained the one-branch-per-task rule after this task ran. History was left alone because `main` was shared live with the TASK-07 and TASK-09 sessions.
- **Three sessions shared this checkout** during Phase C, which CLAUDE.md now forbids. TASK 09's uncommitted work was present when the Phase-C commit was made; it was preserved byte-for-byte by committing only TASK-08 paths and reconstructing the shared `docs/TASK_LOG.md` around the other session's block. Parallel work needs `git worktree add`.
- **The human's HPC sweep commits carry mixed task material** (`2d2c525`, `fd98185`). Read TASK-08 numbers from `results/runs/ft_*/`, not from a commit label.
- **Numbers come from the val-selected `best.pth`.** No `*_best` vs `*_last` ambiguity exists here — there is one test evaluation per run and it uses `best.pth`.
- `evaluation/e6_finegrained/data/{cub,aircraft}_dataset.py` are the LEGACY loaders. Only their `stage_*` tar-extraction helpers are reused; their `Dataset` classes are not, and their label logic was re-implemented once in `ft_meta.py` so the builder and trainer cannot disagree.
- Three housekeeping edits live outside e6 + new tooling: a `.gitignore` rule for `results/smoke/`, one line in `results/README.md` documenting `ftsplit/`, and a new `.gitattributes` forcing LF on `*.sbatch` (a CRLF sbatch fails cryptically on the cluster).

