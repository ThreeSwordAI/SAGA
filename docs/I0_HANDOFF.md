# TASK I0 handoff — the frozen-intervention framework, the eligible cohort, and the locked protocol

**Status: COMPLETE (Phases A / B / C). Nothing pending from the HPC.**  
GENERATED by `analysis/build_I0_handoff.py` at git `e3c2f7f`. Every number below is read from `results/frozen/I0_manifest/`, `results/frozen/splits/` and `results/diagsplit/`; none is typed by hand. The surrounding prose is fixed text. Re-run the script after new results land and this document updates itself.

I0 concluded nothing scientific, by design. It exists so that I1-I7 share one definition of "feature stage", one of "prefix tokens", one cohort, one set of splits, and one guarantee that a temporary edit was temporary. Everything scientific downstream depends on it.

## 0. Status at a glance

| phase | what | state |
|---|---|---|
| A | manifest, splits builder, framework, smoke checker, hash tool, 101 test functions | **done** |
| B | HPC: split build, checkpoint hashes, 32-image smoke on three checkpoints | **done** |
| C | handoff, `LOCKED_ANALYSIS` split shas, final row counts | **done** |

TASK I0 contributes 101 test functions (47 frozen, 34 manifest, 20 splits); the repo-wide total is deliberately not quoted here, since it drifts with every other task.

## 1. What a later work package actually uses

```python
from saga.frozen import (STAGES, HIST_STAGE, capture_stages,
                         terminal_gate_override, gate_edit,
                         receiver_perturbation, state_hash)
```

| you want | use |
|---|---|
| patch tokens at a named stage | `capture_stages(model, stages)` — prefix rows already removed with the MODEL's own count |
| the terminal patch-gate control (I2) | `terminal_gate_override(model, value)` |
| gate mean / arrangement edits (I3) | `gate_edit(model, layer, mode, alpha=, perm=)` |
| masked receiver perturbation (I4) | `receiver_perturbation(model, layer, mask, epsilon, energy_target=)` |
| to run a whole condition sweep | `tools/frozen_eval.py --run-id ... --conditions configs/frozen/<wp>.yaml --split ...` |

Every edit is a context manager that sha256-hashes every parameter and buffer on entry and on exit and REFUSES to exit if they differ. Every patch operation excludes prefix rows using `infer_num_prefix_tokens(model)` — never a hard-coded 1. All of it works on the timm 4-register model **without** wrapping it in `SAGAViT`, which refuses `num_prefix_tokens != 1` by design.

**The conditions contract.** No condition, layer, epsilon, mask or permutation is selectable from the command line; they live in a per-work-package YAML under `configs/frozen/`, committed before the job runs. That is what makes "the analysis configuration was fixed before the outcomes were inspected" a property of this repository rather than a promise. I0 ships `configs/frozen/smoke.yaml` and nothing else.

## 2. The eligible cohort

**19 completed 300-epoch conditions.** One row per condition; the manifest carries 124 rows in total, one per saved checkpoint or checkpoint-derived condition.

| arch | recipe_actual | baseline | registers | saga | total |
|---|---|---|---|---|---|
| vit_base | mixup | 2 | 1 | 2 | 5 |
| vit_small | mixup | 4 | 2 | 4 | 10 |
| vit_small | nomix | 2 | 0 | 2 | 4 |
| **total** | | 8 | 3 | 8 | **19** |

10 of the 19 were trained under the seeded e2r trainer (`seed_controlled=1`); the remaining 9 are legacy repeats whose trainer recorded no seed (`seed=MISSING`).

**`recipe_actual` is never a directory name.** Cell membership is IMPORTED from `analysis/build_pooled_tables.py`, so the recipe erratum remap and the exclusion of the VOID legacy ViT-B mixup-dir trio cannot drift between this manifest and `results/tables/e2_pooled.csv`. For the e2r/ablation runs the recipe is read from the run's own resolved config and cross-checked against its augmentation block; a `recipe:` key contradicting its own mixup/cutmix alphas is a hard error.

### Rows per family and status

| family | derived | eligible | eligible_legacy | invalid | superseded | total |
|---|---|---|---|---|---|---|
| e2r_300ep | — | 10 | — | 4 | 10 | 24 |
| legacy_300ep | — | — | 9 | 6 | 9 | 24 |
| ablation_100ep | — | 6 | — | — | 6 | 12 |
| finetune | — | 24 | — | — | 24 | 48 |
| dense_det | — | 3 | — | — | 3 | 6 |
| dense_seg | — | 3 | — | — | 3 | 6 |
| ttr_edit | 4 | — | — | — | — | 4 |
| **total** | 4 | 46 | 9 | 10 | 55 | **124** |

### What is excluded, and why

- **The VOID legacy ViT-B mixup-DIRECTORY trio** (3 conditions: legacy_e2_vit_base_mixupdir_baseline, legacy_e2_vit_base_mixupdir_registers, legacy_e2_vit_base_mixupdir_saga) never finished training. It appears in the manifest so it is accounted for, is `invalid` for BOTH checkpoint kinds, and is a member of no cell.
- **2 never-started e2r ViT-B s2 runs** (e2r_vitb_mixup_baseline_s2, e2r_vitb_mixup_saga_s2) logged one epoch of 300 with a null `end_time`.
- **Every `best.pth` outside the fine-tuning family** is `superseded`: diagnostics come from the LAST checkpoint everywhere except fine-tuning, which selects on a frozen val split and whose `best.pth` is therefore its canonical checkpoint.

## 3. The historical feature stage — identified, not assumed

Every historical patch diagnostic in this project was computed at **`s12_pre_norm`**: the output of the LAST transformer block, **before** the final LayerNorm. `saga/frozen/stages.py` exposes it under the alias `hist`, so a later file can write `stage: hist` and mean "whatever the historical diagnostics used".

> saga/metrics.py:287-291 (forward hooks on model.blocks[i], storing the block's own output), saga/metrics.py:324-325 (feats[L-1][:, P:, :]), saga/vit.py:238-241 (SAGAViT.forward applies self.norm AFTER the block loop), tools/diagnose.py:89-95 (the historical driver)

This is pinned by `tests/test_I0_frozen.py::test_hist_stage_matches_the_historical_hook`, which RUNS the real `compute_diagnostics` hook and compares tensors rather than asserting a constant — and separately asserts the captured tensor is NOT the post-norm one.

The four stage names, and there is no fifth anywhere in the project: `s11_out`, `s12_pre_norm`, `s12_post_norm`, `hist`.

## 4. The locked splits

| split | n | classes | sha256 |
|---|---|---|---|
| discovery (existing, unchanged) | 10,000 | 1,000 | `0a686340c00846818a857cf4cbf472cbdc035e0a88c20d79483f7d9f463b69b1` |
| `calibration.json` | 2,000 | 1,000 | `6b707eb39f934274a5ea753d613af54dc9aad01510808f246b70506a96003003` |
| `evaluation.json` | 10,000 | 1,000 | `7fdf5f9f2ace98ef03a6267455daa1104b92b6689c510ab8b1e5420340f27014` |
| `sub1k.json` | 1,000 | 1,000 | `6a38d1000b32f2ca0c5bcfd1187cde86258b2d45a4b1e4b47cfd5580fe088ef3` |
| `sub2k.json` | 2,000 | 1,000 | `d2fc8b5b4c5ab40928c1ea861f21a3b561c0654a93479048773802568d338117` |

`discovery`, `calibration` and `evaluation` are pairwise disjoint BY CONSTRUCTION — an image claimed by an earlier split is never a candidate for a later one. `sub1k` and `sub2k` are drawn FROM `evaluation` and are checked to be subsets of it. The builder refuses to overwrite a frozen split, and each file carries a `sha256` over its own canonical serialization which is re-verified on every invocation.

**The sentence that must survive into the paper:** The original full-validation accuracies have already been seen, so this is a LOCKED EVALUATION PROTOCOL FOR THE NEW ANALYSES — not a completely untouched test set, and not a retrospectively preregistered study.

## 5. What the smoke checks measured on the real checkpoints

Nine checks on a fixed 32-image subset of `evaluation.json`, in fp32, on 3 checkpoints.

| checkpoint | variant | n_prefix | PASS | FAIL | SKIP |
|---|---|---|---|---|---|
| `e2r_vits_mixup_baseline_s1` | baseline | 1 | 5 | 0 | 4 |
| `e2r_vits_mixup_saga_s1` | saga | 1 | 8 | 1 | 0 |
| `legacy_e2_vit_small_mixupdir_registers` | registers | 5 | 5 | 0 | 4 |

**18 of 19 applicable checks PASS.** A check that cannot apply to a checkpoint — a gate check on a baseline or a register model — is SKIP with its reason, never a silent PASS.

### The measurements that matter

| what | measured |
|---|---|
| Proposition 2 — terminal patch gate vs CLS logits, `e2r_vits_mixup_saga_s1` | **0** at all 4 constants (tolerance 0.001) |
| identity edit, `e2r_vits_mixup_saga_s1` | 32/32 images bit-exact, max abs logit diff 0 |
| prefix rows, `e2r_vits_mixup_baseline_s1` (n_prefix=1) | max abs diff **0** with injected norm 2.8938 over 8 masked coordinates |
| prefix rows, `e2r_vits_mixup_saga_s1` (n_prefix=1) | max abs diff **0** with injected norm 2.32865 over 8 masked coordinates |
| prefix rows, `legacy_e2_vit_small_mixupdir_registers` (n_prefix=5) | max abs diff **0** with injected norm 4.6391 over 8 masked coordinates |
| energy matching, `e2r_vits_mixup_baseline_s1` | max relative error 7.21e-08 against tolerance 0.0001, 0 zero-norm images |
| energy matching, `e2r_vits_mixup_saga_s1` | max relative error 8.96e-08 against tolerance 0.0001, 0 zero-norm images |
| energy matching, `legacy_e2_vit_small_mixupdir_registers` | max relative error 8.99e-08 against tolerance 0.0001, 0 zero-norm images |

The prefix-row check reports the injected norm alongside the difference, so a PASS cannot come from an edit that did nothing. It compares the edited BLOCK's output rather than the attention module's: a forward hook that returns a value replaces the output only for hooks registered AFTER it, so a capture hook registered first would have seen the unedited tensor and the check would have passed while measuring nothing.

### The recorded FAIL, and why it is not a defect in the intervention

`e2r_vits_mixup_saga_s1` records **FAIL** on `permutation_preserves`:

- `layer` = 10
- `max_abs_per_head_mean_diff` = 5.96e-08
- `n_patches` = 196
- `per_head_sorted_values_identical` = True

`permutation_preserves` asserted that the per-head gate MEAN was bit-identical after a permutation. It cannot be: the mean is a reduction over 196 fp32 values in a different order, and fp32 addition is not associative. The measured difference is exactly one ULP at magnitude 1, while `per_head_sorted_values_identical` is `True` — the per-head MULTISET of gate values is bit-identical, which is the invariant that actually holds and which the check still asserts with zero tolerance. The mean now carries a stated 8-ULP tolerance (`tools/frozen_smoke.py::TOL_PERM_MEAN`), which `tests/test_I0_frozen.py` imports rather than restates. **Under the corrected check this recorded measurement is a PASS.** The JSON is left exactly as the HPC wrote it; re-running the job rewrites it with the corrected verdict.

## 6. Checkpoint identity

120 of 124 rows carry a 64-character `ckpt_sha256`; 4 are MISSING, and every one is accounted for (a test permits no unexplained gap).

- **4 rows, all `ttr_edit`** — the test-time-register conditions are inference-time EDITS of a base checkpoint, not models, and have no checkpoint of their own by design. Each records its base run, the neurons-file sha, the layer range and `n_neurons` instead.

**The hashes were cross-checked against two files that recorded checkpoint shas independently, long before this task existed, and both agree:**

- `e2r_vits_mixup_baseline_s1` = `fixed_thresholds_canon.json[vit_small|mixup]`: **True**
- `e2r_vitb_mixup_baseline_s1` = `fixed_thresholds_canon.json[vit_base|mixup]`: **True**
- `e2r_vits_nomix_baseline_s1` = `fixed_thresholds_canon.json[vit_small|nomix]`: **True**
- legacy rows agreeing with `results/legacy/checkpoint_manifest.csv`: **24 of 24**
- the hash pass itself reported `117` hashed of `117` targets

### Detection has no best checkpoint, and what was done about it

`detection/tools/train.py` saves `ckpt/last.pth` only; its best-AP state is the JSON pair `coco_eval_best.json` + `detections_val.json`. The Phase-B hash pass found this empirically — six `best.pth` paths that never existed — and the filenames now come from a table justified against the trainers' source (segmentation writes `best_model.pth`, not `best.pth`).

For 3 of 3 detection runs the `best` row RESOLVES to `ckpt/last.pth`, because the run's own files prove they are the same thing:

| run | best-AP epoch | last epoch | resolves |
|---|---|---|---|
| `det_vitb_baseline_s1` | 24 | 24 | last |
| `det_vitb_registers_s1` | 24 | 24 | last |
| `det_vitb_saga_s1` | 24 | 24 | last |

The epochs are read from `coco_eval_best.json` and `log.csv` on every build; a run that peaked earlier would NOT resolve and would state that its best weights were never saved. The cost — two `ckpt_kind`s of one run sharing a sha256 — is declared in `derived_params.resolves_to_ckpt_kind`, and only the `last` row is `eligible`, so no count over eligible rows can double-count a run.

## 7. What I0 did NOT settle

`docs/LOCKED_ANALYSIS.md` is a **DRAFT**. It carries three `PENDING` lines (signed by / date frozen / git sha at freeze); until the human fills them in, nothing in it is locked and **no I3/I4 Phase-B job may run**. 7 of 8 decisions are open.

| # | § | question | default if unanswered |
|---|---|---|---|
| D1 | 1 | Which stage for all new patch diagnostics? | `hist` (= `s12_pre_norm`), pending I2 |
| D2 | 3 | Which dihedral subset — all 8, or a smaller prespecified set? | all 8 |
| D3 | 4 | The ten fixed permutation index lists are not yet generated or committed | `RandomState(0..9).permutation(196)`, committed by I3 Phase A to `configs/frozen/permutations_14x14.json` |
| D4 | 5 | Map basis for the I4 prevalence mask: `canon` or `mad`? | `canon` |
| D5 | 5 | The discovery map at the INPUT to block 7/8 does not exist — the committed `*_addr.json` maps are last-block. I1 must produce it before I4 Phase B | blocks I4; no default |
| D6 | 8 | Bootstrap seed | `0` |
| D7 | 9 | Multiplicity correction across the three primary contrasts | none, with all three named in advance |
| ~~D8~~ | 11 | ~~The five split shas~~ | **CLOSED** — filled in from the Phase-B build, 2026-09-16 |

1 closed in Phase C (D8): the split shas in §11 are filled in from the Phase-B build.

**1 of the open decisions BLOCKS a work package.** D5: I4 needs a prevalence map at the INPUT to the edited block, and the committed `*_addr.json` maps are last-block, so I1 must produce it before I4 Phase B. The rest can be defaulted, and the defaults are written down so that defaulting is a visible decision rather than a silent one.

## 8. Rules every later work package inherits

1. **Nothing overwrites a historical result, note, table or threshold.** New keys only. `saga/gate.py`, `saga/vit.py`, `saga/metrics.py`, `tools/eval.py` and `tools/diagnose.py` were not touched by I0 at all.
2. **`MISSING` is a value.** It is never averaged and never replaced by a guess.
3. **Every record row carries** checkpoint sha256, split sha256, stage, prefix count, precision and git sha. A number that cannot be traced to a checkpoint and a split is not a number this project reports.
4. **The runner REFUSES** a manifest row whose `ckpt_sha256` is MISSING, a checkpoint whose file hash has changed, and a split whose content no longer matches its recorded sha.
5. **Writers are append-safe.** A resubmitted job adds what is missing and rewrites nothing; the completion marker is written last.
6. **A generated file under `results/` is REGENERATED, never hand-merged.** Two sides regenerating the same artifact from different inputs is the normal state whenever a phase runs on the HPC and a fix lands locally.
7. **No training, no optimizer, no probe fitting** in any frozen file. An AST-level test enforces it.

## 9. Where everything lives

| what | path |
|---|---|
| framework | `saga/frozen/{stages,edits,records,runner}.py` |
| drivers | `tools/frozen_eval.py`, `tools/frozen_smoke.py`, `tools/frozen_manifest_hashes.py`, `tools/build_frozen_splits.py` |
| manifest builder | `analysis/build_I0_manifest.py` |
| this document's generator | `analysis/build_I0_handoff.py` |
| cohort | `results/frozen/I0_manifest/{manifest.csv,manifest.json,eligibility.md,ckpt_hashes.json}` |
| smoke results | `results/frozen/I0_manifest/smoke_<run_id>.json` |
| splits | `results/frozen/splits/` |
| locked parameters | `docs/LOCKED_ANALYSIS.md` (DRAFT) |
| job file | `scripts/jobs/frozen_smoke.sbatch` |
| conditions | `configs/frozen/smoke.yaml` |
| tests | `tests/test_I0_{frozen,manifest,splits}.py` |

